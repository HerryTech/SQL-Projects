CREATE TABLE books(
book_id VARCHAR PRIMARY KEY,
title VARCHAR(100),
author VARCHAR(100), 
genre VARCHAR(50),
publish_year DATE
);

CREATE TABLE borrowers(
borrower_id	VARCHAR PRIMARY KEY
Name VARCHAR(100)	
Email VARCHAR(100)
Join_date DATE
);
